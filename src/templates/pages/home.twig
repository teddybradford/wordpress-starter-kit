{% extends "main.twig" %}

{% block content %}
  <h1>Hello, world!</h1>

  {% for post in posts %}
    <article>
      <a href="{{ post.link }}">{{ post.title }}</a>
    </article>
  {% endfor %}
{% endblock %}
